const { Given, When, Then } = require('@cucumber/cucumber');

Given('que el usuario está en la landing', function() {
    // Asegurar carga de la página principal
});

When('hace clic en “Descargar para Android”', function() {
    // Simular clic en el botón Android
});

Then('se abre la página de FaceLoock en Google Play', function() {
    // Verificar URL de Google Play
});

When('hace clic en “Descargar para iOS”', function() {
    // Simular clic en el botón iOS
});

Then('se abre la página de FaceLoock en el App Store', function() {
    // Verificar URL de App Store
});
